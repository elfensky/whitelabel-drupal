/**
 * @file
 * whitelabel behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.whitelabel = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
